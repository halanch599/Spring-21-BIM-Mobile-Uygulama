<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bim_1_1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BIM_1_1">
        <activity android:name=".CustomerAddActivity"></activity>
        <activity android:name=".CustomerListViewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SharedPreferrencesActivity" />
        <activity android:name=".model.NavigationActivity" />
        <activity android:name=".TabbedActivity" />
        <activity android:name=".RecyclerActivity" />
        <activity android:name=".ProductCustomListViewActivity" />
        <activity android:name=".YemekActivity" />
        <activity android:name=".ActivityCalculator" />
        <activity android:name=".ActivityRegister" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>